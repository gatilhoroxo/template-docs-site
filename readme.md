# Template Docs Site

O objetivo é fazer um site de documentação do zero, sem o uso de IA em nenhum processo de desenvolvimento. 

## Pré-requisitos

- Ruby 2.7 ou superior
- Bundler
- Jekyll 3.x ou superior
- Git

```sh
sudo apt install ruby-dev
sudo snap install ruby --classic
gem install jekyll
sudo apt install git
```
